# R-3.3 — Swarm vs Single-Call A/B Quality Report

> Generated by `scripts/check-swarm-quality.mjs` on 2026-05-02T15:42:26.049Z.
> Mode: **MOCK (no credentials resolved)**

## ⚠️ Mock-mode run

No `ANTHROPIC_API_KEY` (env or Secret Manager) was resolved at run time. Both paths returned the canned 3-finding mock response, so the numbers below are wiring-only — they do NOT reflect actual swarm-vs-single-call quality.

**To run the real A/B**: set `ANTHROPIC_API_KEY` (or configure gcloud Secret Manager per ADR-093) and re-run `tsx scripts/check-swarm-quality.mjs`.

## Aggregate (3 seed goals)

| Metric | Single-call | Swarm | Delta |
|---|---:|---:|---:|
| Total findings | 9 | 9 | 0 |
| Total unique sources | 9 | 9 | 0.0% |
| Mean confidence | 0.80 | 0.80 | 0.00 |
| Mean latency (ms) | 0 | 0 | 0.14× |

## DoD targets (ADR-096)

- ≥30% more unique citations vs single-call: **⏳ pending real-LLM run**
- ≥20% reduction in hallucination rate (Anthropic-Opus judged): **⏳ deferred** (judge step not implemented in this harness; follow-up)

## Per-goal

### Best electric vehicle under $50k for a family of 4

| Path | Findings | Unique sources | Mean conf | Latency (ms) |
|---|---:|---:|---:|---:|
| single-call | 3 | 3 | 0.80 | 0 |
| swarm | 3 | 3 | 0.80 | 0 |

### How does GOAP planning compare to behavior trees in real-time games

| Path | Findings | Unique sources | Mean conf | Latency (ms) |
|---|---:|---:|---:|---:|
| single-call | 3 | 3 | 0.80 | 0 |
| swarm | 3 | 3 | 0.80 | 0 |

### Quantum computing applications in pharmaceutical drug discovery

| Path | Findings | Unique sources | Mean conf | Latency (ms) |
|---|---:|---:|---:|---:|
| single-call | 3 | 3 | 0.80 | 0 |
| swarm | 3 | 3 | 0.80 | 0 |

## Honest caveats

1. The Anthropic-Opus hallucination judge step is deferred to a polish PR — needs a separate Opus prompt + scoring rubric. R-3.3's wiring + counting infrastructure is shipped; the judge slot is intentionally a follow-up.
2. With real LLM creds, latency is ≈3-5s per step single-call and ≈12-20s swarm. The latency multiple in the table above will reflect that ratio.
3. Unique-source counting normalizes by lowercase + trim. URL canonicalization (e.g. trailing slash, query params) is not done — if the LLM emits the same source with minor variants, this overcounts. A canonicalizer would tighten the metric; deferred.
